import { FormsNoCatePipe } from './forms-no-cate.pipe';

describe('FormsNoCatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormsNoCatePipe();
    expect(pipe).toBeTruthy();
  });
});
