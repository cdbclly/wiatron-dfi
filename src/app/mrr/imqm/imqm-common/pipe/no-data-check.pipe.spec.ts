import { NoDataCheckPipe } from './no-data-check.pipe';

describe('NoDataCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new NoDataCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
