import { GroupTransPipe } from './group-trans.pipe';

describe('GroupTransPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTransPipe();
    expect(pipe).toBeTruthy();
  });
});
