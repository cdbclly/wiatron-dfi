import { NewmodelMaintainPipe } from './newmodel-maintain.pipe';

describe('NewmodelMaintainPipe', () => {
  it('create an instance', () => {
    const pipe = new NewmodelMaintainPipe();
    expect(pipe).toBeTruthy();
  });
});
