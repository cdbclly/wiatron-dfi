import { KpiFilterPipe } from './kpi-filter.pipe';

describe('KpiFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KpiFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
