import { MrrDocBomFilterPipe } from './mrr-doc-bom-filter.pipe';

describe('MrrDocBomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MrrDocBomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
