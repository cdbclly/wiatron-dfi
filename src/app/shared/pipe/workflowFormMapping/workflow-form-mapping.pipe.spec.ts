import { WorkflowFormMappingPipe } from './workflow-form-mapping.pipe';

describe('WorkflowFormMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkflowFormMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
