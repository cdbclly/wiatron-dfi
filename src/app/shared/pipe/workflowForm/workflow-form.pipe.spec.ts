import { WorkflowFormPipe } from './workflow-form.pipe';

describe('WorkflowFormPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkflowFormPipe();
    expect(pipe).toBeTruthy();
  });
});
