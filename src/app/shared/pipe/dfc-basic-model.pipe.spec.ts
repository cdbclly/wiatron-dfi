import { DfcBasicModelPipe } from './dfc-basic-model.pipe';

describe('DfcBasicModelPipe', () => {
  it('create an instance', () => {
    const pipe = new DfcBasicModelPipe();
    expect(pipe).toBeTruthy();
  });
});
