import { MrrDocDelayedDayPipe } from './mrr-doc-delayed-day.pipe';

describe('MrrDocDelayedDayPipe', () => {
  it('create an instance', () => {
    const pipe = new MrrDocDelayedDayPipe();
    expect(pipe).toBeTruthy();
  });
});
