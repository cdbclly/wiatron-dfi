import { PlantModelPipe } from './plant-model.pipe';

describe('PlantModelPipe', () => {
  it('create an instance', () => {
    const pipe = new PlantModelPipe();
    expect(pipe).toBeTruthy();
  });
});
