import { DFiProductPipe } from './dfiProduct.pipe';

describe('DFiProductPipe', () => {
  it('create an instance', () => {
    const pipe = new DFiProductPipe();
    expect(pipe).toBeTruthy();
  });
});
