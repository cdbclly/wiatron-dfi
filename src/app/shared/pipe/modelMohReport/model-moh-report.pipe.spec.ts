import { ModelMohReportPipe } from './model-moh-report.pipe';

describe('ModelMohReportPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelMohReportPipe();
    expect(pipe).toBeTruthy();
  });
});
