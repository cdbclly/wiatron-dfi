import { ModelWorkhourReportPipe } from './model-workhour-report.pipe';

describe('ModelWorkhourReportPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelWorkhourReportPipe();
    expect(pipe).toBeTruthy();
  });
});
