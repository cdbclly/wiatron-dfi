import { UserEnnamePipe } from './user-enname.pipe';

describe('UserEnnamePipe', () => {
  it('create an instance', () => {
    const pipe = new UserEnnamePipe();
    expect(pipe).toBeTruthy();
  });
});
