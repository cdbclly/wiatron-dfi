import { FlowStatusPipe } from './flow-status.pipe';

describe('FlowStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FlowStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
