import { DfiDisableDirective } from './dfi-disable.directive';

describe('DfiDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new DfiDisableDirective();
    expect(directive).toBeTruthy();
  });
});
