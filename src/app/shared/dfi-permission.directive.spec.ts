import { DfiPermissionDirective } from './dfi-permission.directive';

describe('DfiPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new DfiPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
