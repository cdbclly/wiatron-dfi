import { JudgeBooleanResultPipe } from './judge-boolean-result.pipe';

describe('JudgeBooleanResultPipe', () => {
  it('create an instance', () => {
    const pipe = new JudgeBooleanResultPipe();
    expect(pipe).toBeTruthy();
  });
});
