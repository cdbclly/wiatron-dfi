import { JudgeResultPipe } from './judge-result.pipe';

describe('JudgeResultPipe', () => {
  it('create an instance', () => {
    const pipe = new JudgeResultPipe();
    expect(pipe).toBeTruthy();
  });
});
