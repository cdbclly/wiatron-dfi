import { SignStatusPipe } from './sign-status.pipe';

describe('SignStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SignStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
