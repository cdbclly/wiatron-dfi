import { DateLocaleStringPipe } from './date-locale-string.pipe';

describe('DateLocaleStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateLocaleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
